# Test Environment Variables
# Used for multiplayer testing with Socket.IO

# Test server configuration
PORT=3001
NODE_ENV=test
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Socket.IO test configuration
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001

# Test database (if needed for future persistence tests)
# REDIS_URL=redis://localhost:6379/1

# Disable analytics and external services during testing
NEXT_TELEMETRY_DISABLED=1

# Test timeouts
TEST_TIMEOUT=10000
TEST_HOOK_TIMEOUT=15000

# Debug mode (uncomment to enable Socket.IO debug logging)
# DEBUG=socket.io:*
